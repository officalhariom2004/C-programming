//write a c program to calculate area of a rectangle
// using hard cord input
#include <stdio.h>
int main()
{
int length = 3, breath = 4;
int area = length*breath;
 printf("area of rectangle is %d" , area);
 return 0;
}