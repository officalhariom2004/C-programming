#include<stdio.h>
int main()
{
    int principle = 100;
    int rate = 5;
    int time = 3;
    int simpleinterest =(principle * rate * time);
    printf("the value of simple intesr is %d" ,simpleinterest);
    return 0;
}